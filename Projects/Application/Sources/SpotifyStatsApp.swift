import SwiftUI

// SwiftUI App entry disabled in favor of UIKit AppDelegate/SceneDelegate lifecycle.
// Keeping this file for reference; remove from target if desired.

// NOTE: Ensure this file is not part of the build target to avoid duplicate entry points.
// If you keep it in target, you will get two @main/principal class conflicts.

// struct SpotifyStatsApp: App {
//     var body: some Scene {
//         WindowGroup {
//             ContentView()
//         }
//     }
// }
