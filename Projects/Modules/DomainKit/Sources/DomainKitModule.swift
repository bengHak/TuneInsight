import Foundation

// DomainKit 모듈의 기본 소스 파일
// 도메인 레이어 관련 기능들이 여기에 위치합니다.
public final class DomainKitModule {
    public static let shared = DomainKitModule()
    
    private init() {}
    
    public func configure() {
        // 도메인 관련 설정을 위한 기본 코드
    }
}
